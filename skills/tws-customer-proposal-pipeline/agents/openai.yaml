interface:
  display_name: "TWS Customer Proposal Pipeline"
  short_description: "Internal proposal strategy phase; requires orchestrator state"
  default_prompt: "Continue only the delegated proposal phase from an existing nokiy-deck-orchestrator run. Require the run directory and a passing route guard, then return structured strategy evidence; do not build or deliver a PPTX."
